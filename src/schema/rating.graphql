# Defines the GraphQL schema.
# Author: Beatriz Sanssi

type Rating {
  _id: ID!
  userId: Int!
  rating: Float!
  movieId: ID!
}

extend type Query {
  # ratings: [Rating]
  ratings(movieId: ID!): [Rating]
}