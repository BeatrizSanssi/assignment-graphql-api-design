# Defines the GraphQL schema.
# Author: Beatriz Sanssi

type Actor {
  _id: ID!
  id: Int!
  name: String!
  movies_played: Int!
  movies: [Movie]  
}

extend type Query {
  actors(id: Int): [Actor]
}